import { Icon } from './Icon';
export type HeaderAction='specification'|'comparison'|'selection'|'catalog'|'quote';
export function Header({compareCount,onAction}:{compareCount:number;onAction:(action:HeaderAction)=>void}){return <header className="selector-header"><div className="brand"><h1>Программа подбора насосов</h1><p>Подберите насос по задаче или инженерным параметрам</p></div><nav aria-label="Основная навигация"><button onClick={()=>onAction('specification')}><Icon name="spec"/>Спецификация</button><button onClick={()=>onAction('comparison')}><Icon name="compare"/>Сравнение <span className="counter">{compareCount}</span></button><button className="active" onClick={()=>onAction('selection')}><Icon name="search"/>Подбор</button><button onClick={()=>onAction('catalog')}><Icon name="catalog"/>Каталог</button><button className="quote" onClick={()=>onAction('quote')}><Icon name="cart"/>Получить КП</button></nav></header>}
