import type { PumpResult,SelectionContext } from '../domain/types';import { QHChart } from './QHChart';
export function SelectionGraph({context,items,onOpen}:{context:SelectionContext;items:PumpResult[];onOpen:()=>void}){const pumps=items.filter(item=>item.level!=='excluded').slice(0,3);return <section className="selection-graph card"><div className="graph-heading"><div><span className="graph-eyebrow">Подбор по рабочей точке</span><h2>Q-H кривые подходящих насосов</h2><p>Сравниваем точку Q {context.q} м³/ч · H {context.h} м с рабочими диапазонами выбранного типа.</p></div><div className="duty-badge"><span>Рабочая точка</span><b>Q {context.q}</b><i>H {context.h}</i></div></div><div className="curve-tabs"><button className="active">Q-H</button><button disabled>КПД</button><button disabled>NPSH</button><button disabled>Мощность</button></div><QHChart context={context} pumps={pumps} embedded/><button className="open-curve-workspace" onClick={onOpen}>Открыть детальный экран кривых →</button></section>}
