import type { PumpResult } from '../domain/types';
export function RightRail({items,compared,onRemove,onComparison,onPurpose}:{items:PumpResult[];compared:string[];onRemove:(id:string)=>void;onComparison:()=>void;onPurpose:()=>void}){const selected=items.filter(item=>compared.includes(item.id));return <aside className="right-rail"><section className="side-card card"><div className="panel-title split">Текущее сравнение <span className="counter muted-counter">{selected.length}</span><button type="button" className="rail-link" onClick={onComparison}>Открыть</button></div>{selected.length?selected.map((pump,index)=><div className="compare-row" key={pump.id}><i>{index+1}</i><span>{pump.name}</span><b>{pump.score}%</b><button className="remove-compare" onClick={()=>onRemove(pump.id)} aria-label="Убрать">×</button></div>):<p className="comparison-empty">Отметьте модели в результатах, чтобы сравнить их.</p>}</section><section className="help-card card"><div className="engineer-avatar">ИИ</div><div><strong>Не уверены в параметрах?</strong><p>Помощник уточнит назначение и среду, не перегружая инженерными полями.</p><button type="button" onClick={onPurpose}>Подобрать по назначению</button></div></section><section className="side-card next card"><h3>Что дальше?</h3>{['Выберите подходящий насос','Откройте карточку','Добавьте в спецификацию'].map((text,index)=><div key={text}><i>{index+1}</i><p><b>{text}</b><small>{index===0?'Сравните вердикты и кривые':index===1?'Проверьте характеристики и TCO':'Сформируйте коммерческое предложение'}</small></p></div>)}</section></aside>}
