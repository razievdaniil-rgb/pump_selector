import { pumpTypes } from '../domain/mockData';
import { Icon } from './Icon';
export function PumpTypeStep({selected,onSelect,onBack,onContinue}:{selected:string;onSelect:(value:string)=>void;onBack:()=>void;onContinue:()=>void}){return <main className="type-shell card"><button className="back-link" onClick={onBack}><Icon name="back" size={16}/> Назад к сценариям</button><div className="step-heading"><span className="eyebrow">Шаг 1 из 3</span><h2>Выберите тип насоса</h2><p>Тип определяет доступные параметры и алгоритм инженерной проверки.</p></div><div className="pump-types">{pumpTypes.map(([name,description])=><button key={name} className={selected===name?'pump-type selected':'pump-type'} onClick={()=>onSelect(name)}><span className="type-icon"><Icon name="pump" size={24}/></span><strong>{name}</strong><small>{description}</small>{selected===name&&<span className="selected-check"><Icon name="check" size={14}/></span>}</button>)}</div><div className="step-actions"><button className="button ghost" onClick={onBack}>Назад</button><button className="button primary" onClick={onContinue}>Продолжить к параметрам</button></div></main>}
