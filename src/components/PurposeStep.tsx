import { purposePresets } from '../domain/mockData';
import type { PurposePreset } from '../domain/types';
import { Icon } from './Icon';
export function PurposeStep({onBack,onSelect}:{onBack:()=>void;onSelect:(preset:PurposePreset)=>void}){return <main className="purpose-shell card"><button className="back-link" onClick={onBack}><Icon name="back" size={16}/> Назад к сценариям</button><div className="step-heading"><span className="eyebrow">Простой подбор</span><h2>Где будет работать насос?</h2><p>Выберите понятный вариант. На следующем экране можно проверить и изменить предложенные параметры.</p></div><div className="purpose-grid">{purposePresets.map(preset=><button className="purpose-card" key={preset.id} onClick={()=>onSelect(preset)}><span className="purpose-icon"><Icon name={preset.id==='boiler-hot-water'?'heat':'purpose'} size={22}/></span><span className="purpose-object">{preset.object}</span><strong>{preset.label}</strong><small>{preset.medium} · {preset.description}</small><span className="scenario-action">Продолжить →</span></button>)}</div><div className="purpose-help"><Icon name="sparkles"/><span><b>Не нашли свой случай?</b> Перейдите к инженерному режиму Q/H или обратитесь к помощнику.</span></div></main>}
