import { chromium } from 'playwright-core';
const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});const page=await browser.newPage({viewport:{width:1440,height:920}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('https://razievdaniil-rgb.github.io/pump_selector/?release=b505969',{waitUntil:'networkidle'});await page.getByRole('button',{name:/Знаю, куда нужен насос/}).click();await page.getByRole('button',{name:/Котельная/}).click();await page.getByRole('button',{name:'Подобрать насос',exact:true}).first().click();await page.locator('.tabs button').filter({hasText:'Не подходят'}).click();const card=page.locator('.result-card').first();const metric=await card.evaluate(node=>{const line=node.querySelector('.status-copy>span:last-child');const duty=node.querySelector('.duty');const range=document.createRange();range.selectNodeContents(line);const text=range.getBoundingClientRect(),d=duty.getBoundingClientRect();return{status:line.textContent,textRight:text.right,dutyLeft:d.left,gap:d.left-text.right,bodyOverflow:document.body.scrollWidth-innerWidth,css:[...document.styleSheets].map(x=>x.href).filter(Boolean)}});await card.screenshot({path:'qa-public-excluded-b505969.png'});console.log(JSON.stringify({errors,metric},null,2));if(errors.length||metric.gap<4||metric.bodyOverflow>0)throw new Error('Public Pages QA failed');await browser.close();
