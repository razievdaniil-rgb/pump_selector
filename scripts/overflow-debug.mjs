import { chromium } from 'playwright-core';
const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});const page=await browser.newPage({viewport:{width:390,height:844}});await page.goto('http://127.0.0.1:5173',{waitUntil:'networkidle'});await page.getByRole('button',{name:/Знаю, куда нужен насос/}).click();await page.getByRole('button',{name:/Котельная/}).click();const bad=await page.evaluate(()=>[...document.querySelectorAll('*')].map(el=>{const r=el.getBoundingClientRect();return{tag:el.tagName,cls:el.className?.baseVal||el.className||'',left:r.left,right:r.right,width:r.width,scroll:el.scrollWidth,text:(el.textContent||'').trim().slice(0,50)}}).filter(x=>x.right>innerWidth+1||x.left< -1).sort((a,b)=>b.right-a.right).slice(0,30));console.log(JSON.stringify(bad,null,2));await browser.close();
