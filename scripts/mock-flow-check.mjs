import { chromium } from 'playwright-core';
const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('dialog',d=>d.dismiss());await page.goto('http://127.0.0.1:5173',{waitUntil:'networkidle'});
const q=page.locator('input[type=number]').first();await q.fill('95');await page.getByRole('button',{name:'Подобрать насос'}).click();await page.waitForTimeout(100);const excludedCount=await page.getByRole('button',{name:/Не подходят/}).textContent();await page.getByRole('button',{name:/Не подходят/}).click();const excludedCards=await page.locator('.result-card.excluded').count();await page.locator('.tabs button').first().click();const boxes=page.locator('.product-actions input');if(await boxes.count()){await boxes.first().check()}const compared=await page.locator('.compare-row').count();await page.locator('.sort select').selectOption('power');const state=await page.evaluate(()=>({context:localStorage.getItem('apgs-selection-context'),compared:localStorage.getItem('apgs-compared'),overflow:document.body.scrollWidth-innerWidth}));console.log(JSON.stringify({errors,excludedCount,excludedCards,compared,state},null,2));if(errors.length||!excludedCards||state.overflow>1)throw new Error('mock flow failed');await browser.close();